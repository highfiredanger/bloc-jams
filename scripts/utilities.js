var pointsArray = document.getElementsByClassName('point');

function forEach(array){
    for (var i = 0; i < array.length; i++){
        array[i];
        // callback here, but what function? 
    }
}

forEach(pointsArray);